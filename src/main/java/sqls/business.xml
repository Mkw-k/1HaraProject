<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="Business">
  	<select id="getId" parameterType="bit.com.a.dto.BusinessDto" resultType="java.lang.Integer">
		SELECT NVL(COUNT(*), 0)
		FROM BUSINESS
		WHERE COMPANYID=#{companyid}
	</select>
	
	<insert id="addmember" parameterType="bit.com.a.dto.BusinessDto">
		INSERT INTO BUSINESS(COMPANYID, PWD, COMPANYNAME, BUSI_NAME, COMADDRESS, ADDRESSDETAIL, AUTH, DEL)
		VALUES(#{companyid}, #{pwd}, #{companyname}, #{busi_name}, #{comaddress}, #{addressdetail}, 2, 0)
	</insert>
		
	<select id="login" parameterType="bit.com.a.dto.BusinessDto" resultType="bit.com.a.dto.BusinessDto">
		SELECT COMPANYID, COMPANYNAME 
		FROM BUSINESS
		WHERE MEMBERID=#{companyid} AND PWD=#{pwd}
	</select>
  </mapper>